#include <iostream>
#include <string>
#include <algorithm>
#include <cmath>
#include <vector>
#include <map>
#include <unordered_map>
#include <set>
#include <unordered_set>
using namespace std;

#define FastIO ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

typedef long long ll;
typedef vector<int> vi;

void solve(){
    int n, U, R, D, L; 
    cin >> n >> U >> R >> D >> L;
    
    bool result;
    if(U <= n-2 && R <= n-2 && D <= n-2 && L <= n-2) result = true;
    else{
        
    }

    
    
}

int main(){
    FastIO
    int t; cin >> t;
    while(t--){
        solve();
    }
}