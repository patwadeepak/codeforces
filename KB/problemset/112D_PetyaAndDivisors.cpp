#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <cmath>
using namespace std;

#define FastIO ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

typedef long long ll;
typedef vector<int> vi;

int main(){
    FastIO
    
    int n; cin >> n;
    vector<vector<int>> x;
    int y;
    for(int i=0; i<n; i++){
        cin >> x[i][0] >> y;
        cout << x[0][0] <<" " << y;
        
        // save all the divisors of xi
        

        // loop yi times backwards to finds any x_prev divisible by any xi divisor

    }
    
}